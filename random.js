const inputHigh = prompt("please inter the number");
const highNumber = parseInt(inputHigh);
const randomNumber = Math.floor(Math.random() * inputHigh) + 1;
// console.log(`${inputHigh} is your number. Computer random number is ${randomNumber}`);
document.write(`${inputHigh} is your number. Computer random number is ${randomNumber}`);










